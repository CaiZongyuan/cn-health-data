# Terminology Model

Canonical datasets use domain-specific tables rather than a universal concept
table. Cross-system relationships are stored as independently versioned
mappings with relation, method, confidence, provenance, and review status.

Mappings must never overwrite source terminology or present uncertain
relationships as exact equivalence.
